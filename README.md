# project-2_Romanov_M25-555

# Инструкция по сборке проекта и работе с базой данных

## Сборка и запуск проекта

| Команда          | Описание                              |
|------------------|---------------------------------------|
| `make install`   | Установка зависимостей                |
| `make package-install` | Установка всех необходимых пакетов |
| `make build`     | Сборка проекта                        |
| `make database`  | Запуск игры (базы данных)             |
| `make publish`   | Публикация                            |
| `make lint`      | Проверка кода с помощью ruff          |

## Работа с базой данных

### Управление таблицами

| Команда                                         | Описание                             |
|-------------------------------------------------|--------------------------------------|
| `create_table <имя_таблицы> <столбец1:тип> ...` | Создать таблицу                      |
| `drop_table <имя_таблицы>`                      | Удалить таблицу                      |
| `list_tables`                                   | Показать список всех таблиц          |
| `info <имя_таблицы>`                            | Вывести информацию о таблице         |

### Операции с данными

| Команда                                                                 | Описание                         |
|-------------------------------------------------------------------------|----------------------------------|
| `insert into <имя_таблицы> values (<значение1>, <значение2>, ...)`      | Создать запись                   |
| `select from <имя_таблицы>`                                             | Прочитать все записи             |
| `select from <имя_таблицы> where <столбец> = <значение>`                | Прочитать записи по условию      |
| `update <имя_таблицы> set <столбец1>=<новое_значение> where <условие>`  | Обновить запись                  |
| `delete from <имя_таблицы> where <столбец> = <значение>`                | Удалить запись                   |

### Служебные команды

| Команда   | Описание                 |
|-----------|--------------------------|
| `help`    | Справочная информация    |
| `exit`    | Выйти из программы       |

### Обработка ошибок
Все операции с базой данных защищены декоратором `@handle_db_errors`, который:
- Автоматически обрабатывает исключения
- Предоставляет понятные сообщения об ошибках
- Защищает от сбоев при неправильном вводе

### Подтверждение опасных операций
Критические команды используют декоратор `@confirm_action`:
- `drop_table` — запрашивает подтверждение перед удалением таблицы
- `delete` — запрашивает подтверждение перед удалением записей

## 🎥 Демонстрация asciinema

[![asciicast](https://asciinema.org/a/glL8jVTV3M7MgxvPvZW4TgJog.svg)](https://asciinema.org/a/glL8jVTV3M7MgxvPvZW4TgJog)
